#include "cub3d.h"

int	main(int ac, char **av)
{
	(void) av;
	//Structure definir sur la stack
	if (ac != 2)
		return (1);
	//Parsing
		//if not error msg (function for it)
	//Start_game
}


//arg_1 : structure data (a definir)
//arg_2 : structure game_info (a defnir) -> struct player -> struct camera etc
//bool	start_game()
